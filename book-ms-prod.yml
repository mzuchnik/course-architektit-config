spring:
  datasource:
    driver-class-name: org.postgresql.Driver
    url: jdbc:postgresql://localhost:5432/postgres
    username: ${BOOK_MS_POSTGRES_USERNAME}
    password: ${BOOK_MS_POSTGRES_PASSWORD}
  jpa:
    hibernate:
      ddl-auto: update
  sql:
    init:
      mode: never